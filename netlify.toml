# Déploiement Netlify — glisser-déposer le dossier, ou connecter le repo Git.
[build]
  publish = "."
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
